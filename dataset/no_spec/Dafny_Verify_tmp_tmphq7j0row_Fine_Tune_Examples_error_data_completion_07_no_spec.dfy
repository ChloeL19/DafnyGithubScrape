method main(n: int) returns (a: int, b: int)
{
    var i: int := 0;
    a := 0;
    b := 0;

    while(i < n)
    {
        if(*)
        {
            a := a + 1;
            b := b + 2;
        }
        else
        {
            a := a + 2;
            b := b + 1;
        }

        i := i + 1;
    }
}


